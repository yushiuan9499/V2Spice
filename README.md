# V2Spice
I'm learning to use HSpice.  
However, I don't like the syntax of HSpice, so I write this translator to convert verilog to HSpice.
Currently, only a subset of verilog is supported.

## Usage
```bash
./build/main <verilog_file>
```
